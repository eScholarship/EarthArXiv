<?xml version="1.0" encoding="UTF-8"?>
<doi_batch xmlns="http://www.crossref.org/schema/4.4.2"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" version="4.4.2"
    xsi:schemaLocation="http://www.crossref.org/schema/4.4.2 http://www.crossref.org/schemas/crossref4.4.2.xsd">
    <head>
        <doi_batch_id>{{ batch_id }}</doi_batch_id>
        <timestamp>{{ now|date:"Y-m-d" }}</timestamp>
        <depositor>
            <depositor_name>{{ depositor_name }}</depositor_name>
            <email_address>{{ depositor_email }}</email_address>
        </depositor>
        <registrant>{{ owner }}</registrant>
    </head>
    <body>
        <journal>
            <journal_metadata>
                <full_title>{{ article.journal.title }}</full_title>
                <abbrev_title>{{ article.journal.title }}</abbrev_title>
                <issn media_type="electronic">{{ article.journal.issn }}</issn>
            </journal_metadata>
            <journal_issue>
                <publication_date media_type="online">
                    <month>{{ article.issue.date.month }}</month>
                    <day>{{ article.issue.date.day }}</day>
                    <year>{{ article.issue.date.year }}</year>
                </publication_date>
                <journal_volume>
                    <volume>{{ article.issue.volume }}</volume>
                </journal_volume>
                <issue>{{ article.issue.issue }}</issue>
            </journal_issue>
            <journal_article publication_type="full_text">
                <titles>
                    <title>{{ title|striptags|escape }}</title>
                </titles>
                {% if article.frozenauthors_set.exists %}
                <contributors>
                    {% for a in contributors %}
                    <person_name contributor_role="author" sequence="first">
                        <given_name>{{ a.given_names }}</given_name>
                        <surname>{{ a.last_name }}</surname>
                        {% if a.orcid %}
                        <ORCID>{{ a.orcid }}</ORCID>
                        {% endif %}              
                    </person_name>
                    {% endfor %}
                 </contributors>
                 {% endif %}
                <publication_date media_type="online">
                    <month>{{ article.date_published.month }}</month>
                    <day>{{ article.date_published.day }}</day>
                    <year>{{ article.date_published.year }}</year>
                </publication_date>
                <doi_data>
                    <doi>{{ article.doi }}</doi>
                    <resource>{{ article_url }}</resource>
                </doi_data>
            </journal_article>
        </journal>
    </body>
</doi_batch>
